void main() {
  int class_held = 16;
  int class_attended = 10;
  double attendance_percentage = (class_attended / class_held * 100);

  if (attendance_percentage >= 75){
  print("allow");
  }
   else {
  print("not allow");
  } 
}
